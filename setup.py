from setuptools import setup

setup(
   name='multimodal_contrastive',
   version='0.0',
   description='multimodal contrastive module',
   author='Ziqing Lu',
   author_email='luz21@gene.com',
   packages=['multimodal_contrastive'],
   install_requires=[],
)
